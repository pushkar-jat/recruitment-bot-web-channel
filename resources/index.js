const { welcomeCard,welcomeCardFirst } = require('./card/welcomeCard')
const { firstCard, secondCard,lastCard,countryCard,stateCard,cityCard,experienceCard } = require('./card/card') 

module.exports = {
    welcomeCard,
    welcomeCardFirst,
    firstCard, 
    secondCard,
    lastCard,
    countryCard,
    stateCard,
    cityCard,
    experienceCard
}